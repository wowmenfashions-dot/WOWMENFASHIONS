# Tasks: Contact Us Comments

- [ ] T001: Execute SQL script to create `ContactMessages` table and stored procedures.
- [ ] T002: Create `ContactMessage.cs` model in `Models/`.
- [ ] T003: Create `IContactMessageService.cs` and `ContactMessageService.cs` in `Services/`. Register in `Program.cs`.
- [ ] T004: Redesign `ContactUs.razor` to include the form and hook up submission to the service.
- [ ] T005: Redesign `AboutUs.razor` to match the new layout with the 4-column feature highlights.
- [ ] T006: Create `AdminContactMessages.razor` with Tabs for Unread/Read and a `MudTable`.
- [ ] T007: Update `NavMenu.razor` to include the `AdminContactMessages` link for Admins.
